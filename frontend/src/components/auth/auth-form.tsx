"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { useForm } from "react-hook-form";
import { ArrowRight, LoaderCircle } from "lucide-react";
import { toast } from "sonner";
import { FormInput } from "@/components/forms/form-fields";
import { Button } from "@/components/ui/button";
type Values={name:string;email:string;password:string;timezone:string};
export function AuthForm({mode}:{mode:"login"|"register"}){const router=useRouter();const [busy,setBusy]=useState(false);const {register,handleSubmit,formState:{errors}}=useForm<Values>({defaultValues:{timezone:Intl.DateTimeFormat().resolvedOptions().timeZone}});const submit=handleSubmit(async values=>{setBusy(true);try{const payload=mode==="login"?{email:values.email,password:values.password}:values;const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body?.error?.message||"Unable to continue");toast.success(mode==="login"?"Welcome back":"Your workspace is ready");router.push("/app");router.refresh()}catch(error){toast.error(error instanceof Error?error.message:"Unable to continue")}finally{setBusy(false)}});return <form onSubmit={submit} className="space-y-5">{mode==="register"&&<FormInput name="name" label="Your name" placeholder="Ada Lovelace" register={register} required rules={{required:"Enter your name",maxLength:{value:80,message:"Use 80 characters or fewer"}}} error={errors.name?.message}/>}<FormInput name="email" label="Email address" type="email" placeholder="you@company.com" autoComplete="email" register={register} required rules={{required:"Enter your email"}} error={errors.email?.message}/><FormInput name="password" label="Password" type="password" autoComplete={mode==="login"?"current-password":"new-password"} placeholder={mode==="login"?"Your password":"At least 12 characters"} register={register} required rules={{required:"Enter your password",minLength:mode==="register"?{value:12,message:"Use at least 12 characters"}:undefined}} error={errors.password?.message}/>{mode==="register"&&<input type="hidden" {...register("timezone")}/>}<Button className="w-full" size="lg" disabled={busy}>{busy?<LoaderCircle className="size-4 animate-spin"/>:<>{mode==="login"?"Log in":"Create account"}<ArrowRight className="size-4"/></>}</Button><div className="relative py-2 text-center text-xs text-muted-foreground before:absolute before:left-0 before:right-0 before:top-1/2 before:border-t"><span className="relative bg-card px-3">or continue with</span></div><Button asChild type="button" variant="outline" className="w-full"><a href={`${process.env.NEXT_PUBLIC_BACKEND_URL||"http://localhost:3000/api"}/auth/google`}><svg viewBox="0 0 24 24" className="size-4"><path fill="#4285F4" d="M22 12.2c0-.7-.1-1.5-.2-2.2H12v4h5.6a4.8 4.8 0 0 1-2.1 3.1v2.6h3.4c2-1.8 3.1-4.5 3.1-7.5Z"/><path fill="#34A853" d="M12 22c2.8 0 5.2-.9 6.9-2.4l-3.4-2.6c-.9.6-2.1 1-3.5 1-2.7 0-5-1.8-5.8-4.3H2.7v2.7A10 10 0 0 0 12 22Z"/><path fill="#FBBC05" d="M6.2 13.7a6 6 0 0 1 0-3.4V7.6H2.7a10 10 0 0 0 0 8.8l3.5-2.7Z"/><path fill="#EA4335" d="M12 6c1.5 0 2.9.5 4 1.6l3-3A10 10 0 0 0 2.7 7.6l3.5 2.7C7 7.8 9.3 6 12 6Z"/></svg>Google</a></Button><p className="text-center text-sm text-muted-foreground">{mode==="login"?"New to Swiply?":"Already have an account?"} <Link className="font-semibold text-foreground hover:text-primary" href={mode==="login"?"/register":"/login"}>{mode==="login"?"Create an account":"Log in"}</Link></p></form>}
