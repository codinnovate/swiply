import Link from "next/link";const tabs=[['/app/settings','General'],['/app/settings/team','Team'],['/app/settings/ai','AI providers'],['/app/settings/billing','Billing'],['/app/settings/notifications','Notifications']];export default function SettingsLayout({children}:{children:React.ReactNode}){return <div><div className="mb-7"><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">Manage</p><h1 className="mt-2 font-display text-4xl font-semibold">Settings</h1><p className="mt-2 text-sm text-muted-foreground">Your profile, workspace, AI providers, team, and subscription.</p></div><nav className="mb-7 flex gap-1 overflow-x-auto border-b">{tabs.map(([href,label])=><Link key={href} href={href} className="shrink-0 border-b-2 border-transparent px-3 py-3 text-sm font-semibold text-muted-foreground hover:border-primary hover:text-foreground">{label}</Link>)}</nav>{children}</div>}
