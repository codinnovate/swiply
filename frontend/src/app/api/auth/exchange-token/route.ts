import { NextRequest, NextResponse } from "next/server";
import { BACKEND_URL, SESSION_COOKIE } from "@/lib/server-api";
export async function POST(request:NextRequest){const {accessToken}=await request.json() as {accessToken?:string};if(!accessToken)return NextResponse.json({error:{code:"TOKEN_REQUIRED",message:"Missing access token"}},{status:400});const check=await fetch(`${BACKEND_URL}/auth/me`,{headers:{Authorization:`Bearer ${accessToken}`},cache:"no-store"});if(!check.ok)return NextResponse.json({error:{code:"UNAUTHORIZED",message:"Invalid session"}},{status:401});const next=NextResponse.json(await check.json());next.cookies.set(SESSION_COOKIE,accessToken,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:7*24*60*60});return next;}
