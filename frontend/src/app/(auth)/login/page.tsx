import type { Metadata } from "next";import { AuthForm } from "@/components/auth/auth-form";export const metadata:Metadata={title:"Log in"};export default function Login(){return <div className="w-full"><h1 className="font-display text-4xl font-semibold">Welcome back.</h1><p className="mb-8 mt-2 text-muted-foreground">Log in, or reset your password from this page.</p><AuthForm mode="login"/></div>}
