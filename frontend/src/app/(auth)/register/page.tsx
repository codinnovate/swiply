import type { Metadata } from "next";import { AuthForm } from "@/components/auth/auth-form";export const metadata:Metadata={title:"Create account"};export default function Register(){return <div className="w-full"><h1 className="font-display text-4xl font-semibold">Create your studio.</h1><p className="mb-8 mt-2 text-muted-foreground">Start free. Bring your own AI key when you’re ready.</p><AuthForm mode="register"/></div>}
